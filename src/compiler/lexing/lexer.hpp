#ifndef COMPILER_LEXER_HPP
#define COMPILER_LEXER_HPP

#include <cmath>

namespace compiler {
auto test_func(const int x, const int y) -> const int { return std::pow(x, y); }
} // namespace compiler

#endif // !COMPILER_LEXER_HPP
